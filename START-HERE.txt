ROGUE VOICE ASSISTANT — FIXED COMPLETE BUILD

The previous deployment error was caused by an unnecessary "functions"
pattern in vercel.json. This version removes that pattern.

CORRECT STRUCTURE:

api/
  chat.js
app.js
index.html
styles.css
package.json
vercel.json
.env.example

IMPORTANT GITHUB STEPS:

1. Extract this ZIP on your computer.
2. In your GitHub testsite repository, delete the old contents, especially:
   - the old api item/folder
   - any root-level chat.js or chat.mjs
   - the old vercel.json
3. Upload EVERYTHING inside this extracted folder.
4. Confirm GitHub shows api as a folder.
5. Open api and confirm chat.js is inside it.
6. Commit the changes.
7. Wait for Vercel to deploy the new commit.

VERCEL ENVIRONMENT VARIABLES:

GEMINI_API_KEY = your real Gemini API key
GEMINI_MODEL = gemini-3.6-flash

After deployment, test:

https://www.simplytestsite.com/api/chat

Expected result:

{
  "status": "ok",
  "service": "Rogue Gemini API",
  "model": "gemini-3.6-flash",
  "apiKeyConfigured": true
}

If apiKeyConfigured is false, the site deployed correctly but the Gemini key
still needs to be added to Vercel Production and the project redeployed.
